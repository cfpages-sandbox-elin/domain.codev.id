# Suggestions

Newest suggestions go at the top. Times are WIB.

| Date/Time (WIB) | Status | Area | Suggestion | Why it matters | Implementation note |
| --- | --- | --- | --- | --- | --- |
| 2026-07-10 12:00 WIB | ❌ Not implemented | UI performance | Replace per-row `Tooltip` instances with a shared/lazy tooltip host and stop nesting tooltips under the domain-name trigger. | Dense lists mount hundreds of tooltip fibers/listeners; cursor traversal pays React state + layout + portal cost on almost every control. | Keep children plain; mount one portal on hover/focus with content from the active trigger. Details in `docs/PERFORMANCE_AND_RANK_TRACKING_PLAN.md`. |
| 2026-07-10 12:00 WIB | ❌ Not implemented | UI performance | Escalate domain list windowing to a sliding window or true virtualization that unmounts offscreen rows. | Current top-N append never unmounts above-viewport rows, so deep scrolling mounts most of a 600+ list and makes hover lag worse mid-session. | Prefer a dependency-light recycle window first; only add a virtualization library if needed. |
| 2026-07-10 12:00 WIB | ❌ Not implemented | UI performance | Drop `transition-all` and CSS grayscale/saturate filters on domain rows in favor of specific color transitions and border/background muting. | Filters and broad transitions amplify paint cost while the cursor moves across dense rows. | Mute registered targets with opacity/border tokens only; animate colors/background only. |
| 2026-07-10 12:00 WIB | ❌ Not implemented | Rank tracking | Add keyword-first SERP rank tracking: one SERP fetch per keyword config, snapshot JSON in R2/`serp_json`, derived multi-domain positions, rotate ~10 free-tier SERP APIs with user-pasted keys. | Multiple owned sites share keywords; per-domain SERP wastes quota; free tiers need rotation. | Full durable plan in `docs/SERP_RANK_TRACKING.md` (schema, adapters, UI, checklist). Performance prerequisites in `docs/OPTIMIZATION.md` next pass. |
| 2026-07-02 06:27 WIB | ❌ Not implemented | WHOIS lifecycle | Replace the fixed `expiry + 65 days` estimate with registry/TLD-specific lifecycle profiles. | Grace, redemption, and deletion timing varies by registry; one generic estimate can start aggressive monitoring too late for some ccTLDs. | Maintain tested profiles by registrable suffix, expose confidence/source in Schedule, and fall back to continuous conservative monitoring when the registry lifecycle is unknown. |
| 2026-07-02 06:27 WIB | ❌ Not implemented | Domain acquisition | Integrate a registrar backorder or registration API for critical `to-snatch` domains. | Even a reliable 15-minute WHOIS poll plus WhatsApp alert can lose a domain to automated buyers; WHOIS only reveals availability after the event. | Add registrar-specific credentials server-side, require per-domain opt-in and spending limits, and submit an idempotent registration/backorder attempt when a confirmed drop is detected. |
| 2026-07-02 06:27 WIB | ❌ Not implemented | Operations | Persist cron heartbeats and alert when scheduled checks or notification delivery stop running. | Without a heartbeat, an empty schedule and a broken cron look identical until a domain is missed. | Store each run's start/end, selected/checked/failed counts, provider failures, and next expected run; show stale health in the Schedule page and notify through a separate watchdog. |
| 2026-07-02 06:27 WIB | ❌ Not implemented | Architecture | Share one schedule-policy module between the frontend tracker and Edge Function scheduler. | The current TypeScript implementations mirror each other manually and can drift, making the Schedule page disagree with production checks. | Move date/cadence decisions to an environment-neutral shared module covered by one test suite, leaving database-specific work in the Edge Function. |
| 2026-06-26 00:32 WIB | ❌ Not implemented | UI testing | Add mobile viewport screenshot or layout regression checks for the main dashboard, Add Domains modal, settings, categories, and docs. | The app has dense operational UI and many responsive breakpoints; build-only verification will not catch visual wrapping, fixed-position collisions, or overflow regressions. | Use Playwright against representative seeded/cached UI states at 375px and 430px widths, and fail on horizontal overflow or known control overlap. |
| 2026-06-05 12:30 WIB | ✅ Implemented | Tooling | Add an ESLint configuration that matches the existing `pnpm run lint` script. | The script currently exits before checking code because ESLint cannot find a config, so lint cannot protect future refactors. | Added root `.eslintrc.cjs`, kept hooks linting active, allowed existing Edge Function `any`/`@ts-ignore` patterns, and fixed the initial lint findings. |
| 2026-06-05 05:46 WIB | ✅ Implemented | Frontend performance | Add route-level code splitting or manual chunks to keep the production JS bundle below Vite's 500 kB warning threshold. | The build emits a >500 kB minified chunk warning, which can slow first load as the app grows. | Split docs, categories, settings panels, Add Domains, and Integration API into lazy chunks; `pnpm run build` no longer emits the warning. |
| 2026-06-04 22:19 WIB | ✅ Implemented | Categories | Keep auto-category membership caching directional instead of sharing containment results both ways. | Containment is directional: a compound can belong to a shorter base category without the shorter base being pulled into the compound category. A symmetric cache can accidentally undo that distinction. | Changed membership cache keys to preserve `anchor -> base` direction while keeping score caching symmetric. |
| 2026-06-04 21:20 WIB | ✅ Implemented | WHOIS | Add direct RDAP via IANA bootstrap before adding more vendor WHOIS adapters. | It gives the app a no-key structured lookup path and reduces pressure on free monthly API quotas. | Added `rdap-iana` direct RDAP plus RDAP.org fallback in shared WHOIS logic; both normalize RDAP events, statuses, and name servers into the existing `WhoisData` shape. |
| 2026-06-04 17:37 WIB | ✅ Implemented | Data model | Persist Auto Mine name-server rules in Supabase or future D1 user settings. | Current rules live in browser `localStorage`, which is fast and migration-free but will not sync across browsers/devices or external agents. | Added `app_user_settings.auto_mine_rules`, synced it through `supabaseService`, kept localStorage as fallback/cache, and kept Auto Mine applying through existing tag updates. |
| 2026-06-04 16:52 WIB | ✅ Implemented | Categories | Persist editable category names and manual category overrides in the database once the auto-category UI settles. | Current category names are local-only, which is fine for fast iteration but will not follow the user across browsers/devices. Manual overrides will also be needed when similarity heuristics are wrong. | Added synced `app_user_settings.category_name_overrides` and `category_manual_overrides` with localStorage fallback/cache; Categories page can manually include, exclude, and reset category membership, and dashboard grouping/filtering respects the persisted corrections. |
| 2026-06-03 23:03 WIB | ✅ Implemented | UI | Use icon-first dashboard navigation and filter controls. | The header and filters should be faster to scan without repeating long text labels everywhere. | Removed obsolete automation setup warning, added app/docs/account/logout icon controls with shared tooltips, and added icons to domain filters. |
| 2026-06-03 22:54 WIB | ✅ Implemented | WHOIS | Replace broad scheduled checking with targeted expiry/drop-window scheduling. | Free-tier WHOIS quota should be spent only when a domain is near renewal, expiry, or drop timing. | Cron now skips known-expiry domains more than 30 days away, treats `mine` as renewal reminders, gives `to-snatch` drop-window polling, caps checks with `WHOIS_CRON_MAX_CHECKS`, and uses concurrency 6. |
| 2026-06-03 22:15 WIB | ✅ Implemented | UI | Keep operational WHOIS provider details collapsed by default and make tooltips escape parent stacking. | The dashboard should focus on domains first, while provider internals remain available when needed. Tooltips should never be hidden behind rows, panels, or parent containers. | Converted provider panel into a default-collapsed accordion and rendered shared tooltips through a body portal with a top-level z-index. |
| 2026-06-03 22:15 WIB | ✅ Implemented | WHOIS | Add persistent quota-aware provider pre-skipping before calling exhausted providers. | Fallback should avoid repeatedly calling providers that have just reported rate-limit or quota exhaustion, even across separate Edge Function instances. | Added and applied `whois_provider_telemetry`, `claim_whois_provider_attempt(...)`, service-role telemetry wiring, per-minute/monthly checks, APILayer quota persistence, 429 temporary blocking, and in-flight provider balancing. |
| 2026-06-03 22:09 WIB | ✅ Implemented | UI | Replace browser tooltips with instant app tooltips and make domain row actions more scannable. | Native tooltips are delayed and inconsistent; visible warning text and text-heavy buy buttons make rows harder to scan. | Made buy icon-only with registrar tooltip, moved availability warning into the buy tooltip, added distinct Mine/To Snatch icons, and made available domains display/filter as To Snatch without a Mine toggle. |
| 2026-06-03 19:05 WIB | ✅ Implemented | UI | Simplify domain rows to show only domain, status, and expiry by default, with WHOIS details on hover/focus. | The previous row showed too much metadata and was harder to scan. | Added shared `Tooltip`, whois.com domain links, status-based row colors, and hover details for registrar, last check, provider, registry status, and name servers. |
| 2026-06-03 19:05 WIB | ✅ Implemented | WHOIS | Parse, persist, and explain registry statuses and name servers from provider responses. | Values like `autoRenewPeriod` and `clientTransferProhibited` need plain-English explanations. | Added provider parsing, Supabase `domain_statuses` and `name_servers` columns, app writes on add/re-check, scheduled check writes, and tooltip display from persisted DB values. |
| 2026-06-03 18:20 WIB | ✅ Implemented | WHOIS | Support both clean server secret names and existing legacy `VITE_` provider secret names in Edge Functions. | Existing Supabase secrets used `VITE_` prefixes, while server code expected non-`VITE_` names, causing providers to appear missing. | Patched shared WHOIS logic and redeployed `get-whois-providers`, `get-whois`, and `check-domains`. |
| 2026-06-03 14:33 WIB | ✅ Implemented | Docs | Add Supabase UI setup guide for the WHOIS provider dashboard function and backup provider secrets. | The project should be manageable from supabase.com for users who do not want CLI deployment steps. | Added `Supabase UI Setup` to `docs/WHOIS_DASHBOARD.md` and linked it from `docs/WHOIS.md`. |
| 2026-06-03 13:58 WIB | ✅ Implemented | WHOIS | Add dashboard visibility for provider implementation state, quota limits, quota remaining, last success/error, and fallback order. | Wrong/exhausted providers can make all domains look available or unknown. The admin needs provider health before trusting bulk checks. | Added provider registry, `get-whois-providers`, `WhoisProviderPanel`, persistent telemetry tables, live quota/error display, quota-aware pre-skipping, and deployed/validated the Edge Function path. |
| 2026-06-03 13:58 WIB | 🟡 Partially implemented | WHOIS | Add WhoisJSON and IP2WHOIS as backup providers after telemetry exists. | Additional backups reduce single-provider false positives and outages, but should not be added blindly without quota/error tracking. | Added adapters and env keys. Live response mapping validation still pending. |
| 2026-06-03 13:51 WIB | ✅ Implemented | Migration | Delay migration and stabilize the current Supabase-backed app first. | Migrating while domain data is wrong could move bad assumptions into D1. Fix data safety and UI recovery before exporting/migrating. | Updated `AGENTS.md` and `docs/MIGRATION.md` with stabilize-first direction. |
| 2026-06-03 13:51 WIB | ✅ Implemented | Data safety | Stop automatically changing tags to `to-snatch` when a WHOIS provider reports `available`. | A provider false-positive was corrupting owned domains into target domains and making the list look fully available. | Disabled login auto-sync mutation, preserved user-selected tags on add/recheck, and allowed manual tag recovery. |
| 2026-06-03 13:51 WIB | ✅ Implemented | UI | Make domain rows aligned and show `last_checked` for every domain. | The previous wrapped row layout made status, tag, dates, and buy controls feel scattered. Last check visibility is needed to judge stale/wrong data. | Reworked `DomainItem` into a grid row with last-checked metadata, compact icon actions, and hidden irrelevant drop-info action. |
| 2026-06-03 13:51 WIB | ✅ Implemented | Recovery | Add a `Re-check Visible` action for filtered domain recovery. | When many rows have wrong status, clicking re-check on each row is too slow. | Added a sequential visible-list recheck action with confirmation to avoid accidental API quota use. |
| 2026-06-03 13:36 WIB | 🟡 Partially implemented | Migration | Migrate domain database/API/WHOIS/cron to D1 first while keeping Supabase Auth temporarily. | Login currently works; the domain data layer is the broken and frustrating part. This reduces migration risk and solves the real pain first. | Documented in `docs/MIGRATION.md`; implementation still pending. |
| 2026-06-03 13:36 WIB | ✅ Implemented | Docs | Keep future planning/research documentation under `docs/` instead of repository root. | Keeps root focused on app entry/config files and avoids doc sprawl. | Moved migration doc to `docs/MIGRATION.md` and updated `AGENTS.md`. |
| 2026-06-03 13:09 WIB | ✅ Implemented | Sorting | Fix `added-desc` comparator in `src/components/DomainList.tsx`. | The default "Added: Newest" sort currently compares `b.created_at` to itself, so ordering is unreliable. | Changed comparator to `new Date(b.created_at).getTime() - new Date(a.created_at).getTime()`. |
| 2026-06-03 13:09 WIB | ❌ Not implemented | Architecture | Move bulk add processing to a Worker/D1 import job instead of browser-side per-domain loops. | Browser loops are fragile for large lists, tab closes, provider rate limits, and retry handling. | Add `app_import_jobs` and `app_import_items`, then process via Queue/Cron. |
| 2026-06-03 13:09 WIB | ❌ Not implemented | Auth | Replace Supabase Auth with Better Auth only when the Worker API exists. | The frontend currently relies on Supabase session objects and functions; swapping auth without backend API routes will break data access. | Implement `/api/auth/*`, session client, and D1-protected domain routes together. |
| 2026-06-03 13:09 WIB | ❌ Not implemented | Security | Restrict CORS on WHOIS/API routes to production and local dev origins. | Current `get-whois` allows `*`; authenticated routes are safer with explicit origins. | For Workers, validate `Origin` and set `Access-Control-Allow-Origin` conditionally. |
| 2026-06-03 13:09 WIB | ✅ Implemented | Dependencies | Remove CDN import map and extra root `/index.tsx` script from `index.html`. | Vite already bundles npm dependencies; mixed CDN import maps can cause version drift and confusing runtime behavior. | Kept only the Vite `/src/index.tsx` entry and inline favicon. |
| 2026-06-03 13:09 WIB | ✅ Implemented | Cleanup | Delete or repurpose empty/duplicate scaffold files. | Empty `index.tsx`, `types.ts`, duplicate root `hooks/useDarkMode.ts`, and empty `public/favicon.svg` add confusion. | Removed unused empty scaffold files. |
| 2026-06-03 13:09 WIB | ✅ Implemented | Docs | Replace generated `src/docs/all-docs.ts` with build-time markdown imports or a generation script. | New files in `/docs` are not visible in-app unless manually copied into TypeScript strings. | `src/docs/all-docs.ts` now uses `import.meta.glob('../../docs/*.md', { query: '?raw', import: 'default', eager: true })`; removed generated `docs/all-docs.ts`. |
| 2026-06-03 13:09 WIB | ❌ Not implemented | Data model | Add persistent notification records. | Current notifications are in-memory, disappear on refresh, and are not audit-friendly. | Use `app_notification_events` from `docs/DB.md`. |
| 2026-06-03 13:09 WIB | ✅ Implemented | Agent workflow | Add repository-specific AI guidance in `AGENTS.md`. | Future agents should proactively log suggestions and reduce reporting noise while preserving implementation quality. | Created `AGENTS.md` with operating style, suggestion format, and project direction. |
